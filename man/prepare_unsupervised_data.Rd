% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/03_expression_analysis.R, R/04_clustering.R
\name{prepare_unsupervised_data}
\alias{prepare_unsupervised_data}
\title{prepare_unsupervised_data
extract qualified subset data with most variance for unsupervised analysis}
\usage{
prepare_unsupervised_data(
  expressions,
  method = c("MAD", "CV", "DQ", "GUMBEL"),
  mad_top_n = -1,
  mad_top_quantile = 0.75,
  cv_top_n = -1,
  cv_top_mean_quantile = 0.5,
  dq_top_mean_quantile = 0.5,
  dq_top_var_quantile = 0.5,
  gumbel_p_cutoff = 0.1,
  remove_outlier = F
)

prepare_unsupervised_data(
  expressions,
  method = c("MAD", "CV", "DQ", "GUMBEL"),
  mad_top_n = -1,
  mad_top_quantile = 0.75,
  cv_top_n = -1,
  cv_top_mean_quantile = 0.5,
  dq_top_mean_quantile = 0.5,
  dq_top_var_quantile = 0.5,
  gumbel_p_cutoff = 0.1,
  remove_outlier = F
)
}
\arguments{
\item{expressions, }{dataframe, no default, gene expressions table,usually should optimally be clean and filtered by prepare_clean_RNA_sample_info_and_protein_expressions,must be provided}

\item{method, }{character, default="MAD", methods for calculation of variance, and etc, and later used by filtering, CV for coefficient of variance, DQ for quantile of both mean and variance (dual quantile), GUMBLE for gumbel distribution of mad, ATC for accumulated correlation with other genes}

\item{mad_top_n, }{numeric, default=-1, number of genes to be kept with top mad values, if it is -1 keep all genes}

\item{mad_top_quantile, }{numeric, default=0.75, percentage of genes to be kept with top mad values if mad_top_n is -1}

\item{cv_top_n, }{numeric, default=-1, number of genes to be kept with top cv values, if it is -1 keep all genes}

\item{cv_top_mean_quantile, }{numeric, default=0.5, percentage of genes to be kept with top mad values if cv_top_n is -1}

\item{dq_top_mean_quantile, }{numeric, default=0.5, genes to be kept with mean value quantile above defined dq_top_mean_quantile}

\item{dq_top_var_quantile, }{numeric, default=0.5,  genes to be kept with variance value quantile above defined dq_top_var_quantile}

\item{gumbel_p_cutoff, }{numeric, default=0.1, genes with p value of MAD gumbel distribution less than gumbel_p_cutoff will be kept}

\item{remove_outlier, }{logic, default FALSE, whether remove outlier value before calculation of variance, and etc}

\item{atc_top_n, }{numeric, default=-1, number of genes to be kept with top atc values, if it is -1 keep all genes}

\item{atc_cutoff, }{numeric, default=0.2, genes to be kept with atc values more than atc_cutoff}

\item{expressions}{Function argument documented from the legacy interface.}

\item{method}{Option controlling how the function runs.}

\item{mad_top_n}{Function argument documented from the legacy interface.}

\item{mad_top_quantile}{Function argument documented from the legacy interface.}

\item{cv_top_n}{Function argument documented from the legacy interface.}

\item{cv_top_mean_quantile}{Function argument documented from the legacy interface.}

\item{dq_top_mean_quantile}{Function argument documented from the legacy interface.}

\item{dq_top_var_quantile}{Function argument documented from the legacy interface.}

\item{gumbel_p_cutoff}{Numeric tuning parameter used by the existing implementation.}

\item{remove_outlier}{Function argument documented from the legacy interface.}
}
\value{
data.frame. data for unsupervised analysis.

The value returned by the current implementation.
}
\description{
Feature selection for clustering (MAD/CV/DQ/GUMBEL/ATC)
}
\details{
Source provenance: mRNA.R (2026-03-02).

Source provenance: GBM/XL_figures/vDotJun0823/GBMnat/R/funcsGBMnat.R (2025-08-11).
}
\examples{
\dontrun{
prepare_unsupervised_data(expressions = ..., method = ...)
}
}
